{
  "name": "versioningjs",
  "license": "UNLICENSED",
  "branchVersion": "^1.6.0",
  "branchPattern": "1.7.*",
  "distTag": "next",
  "repository": {
    "type": "git",
    "url": "https://github.com/Pemmasani1/Versioning.git"
  },
  "engines": {
    "node": "^6.9.1",
    "yarn": ">=0.21.3",
    "grunt": "^1.2.0"
  },
  "scripts": {
    "clean": "gulp clean",
    "prebuild": "gulp verify",
    "build": "gulp build",
    "deploy": "gulp version-file && gulp version && gulp publish",
    "ci-pullreq": "npm install && npm run prebuild",
    "ci-build": "npm install && npm run clean && npm run build && npm run deploy",
    "lint": "gulp lint",
    "start": "gulp serve"
  },
  "devDependencies": {
   },
  "dependencies": {},
  "commitplease": {
    "style": "angular",
    "nohook": true
  }
  }
}
